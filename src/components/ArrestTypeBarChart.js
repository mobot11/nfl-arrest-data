import React from 'react';
import PropTypes from 'prop-types';

const ArrestTypeBarChart = ({data}) => {
  return (
    <div>
      hello world
    </div>
  );
};

ArrestTypeBarChart.propTypes = {
  
};

export default ArrestTypeBarChart;